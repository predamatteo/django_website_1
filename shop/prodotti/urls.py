from django.urls import path
from . import views

urlpatterns = [
    #path('', views.index, name='home'),
    #path('in-offerta', views.in_offerta),
    #path('contattaci', views.contattaci, name='contattaci')
]
